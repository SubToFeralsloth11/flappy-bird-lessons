# Established: canvas drawing basics

User understands the three web technologies (HTML = skeleton, CSS = skin, JS = brain) and can draw filled rectangles on a `<canvas>` element using `getContext('2d')`, `fillStyle`, and `fillRect()`. User demonstrated this through the sandbox exercises and quiz answers. This is the foundation for every visual element in the game.

Evidence: User completed interactive sandbox exercises and three quiz questions about canvas coordinates and drawing.
Implications: Next lesson can introduce the animation loop (`requestAnimationFrame`), since the user now has a mental model for putting pixels on screen.
